# Copy this file to .env and fill in your values
# Never commit .env — it is git-ignored

# ── Anthropic (all challenges) ──────────────────────────────
ANTHROPIC_API_KEY=

# ── OpenAI (Days 09–11, embeddings) ─────────────────────────
OPENAI_API_KEY=

# ── Pinecone (Days 09–11, vector store) ─────────────────────
PINECONE_API_KEY=
PINECONE_ENVIRONMENT=

# ── LangSmith (Days 11, 19, observability) ───────────────────
LANGSMITH_API_KEY=
LANGCHAIN_PROJECT=ai-labs

# ── AWS (Days 25–27, deployment) ─────────────────────────────
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-south-1

# ── External REST APIs (Days 04, 23) ─────────────────────────
# Add keys here as challenges require them
